def uniqueChar(string): 
    characters = {}
    for char in string:
        if characters.get(char, ): #if present
            if len(string) == 0;
            return None
            
            if char in characters:
                return False
            else: